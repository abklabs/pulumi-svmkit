name: aws-agave-validator-python
description: An Agave validator deployed on AWS using Python
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
    typechecker: pyright
plugins:
  providers:
    - name: svmkit
      path: ../../bin
